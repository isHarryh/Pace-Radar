{
  "name": "@pace-radar/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "node ../../scripts/prepare-wrangler-config.mjs && wrangler deploy --config wrangler.generated.toml"
  },
  "dependencies": {
    "@pace-radar/shared": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.9.3"
  }
}
