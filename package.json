{
  "name": "pace-radar",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @pace-radar/collector-core run test",
    "dev:web": "pnpm --filter @pace-radar/web dev",
    "dev:api": "pnpm --filter @pace-radar/api dev",
    "dev:collector": "pnpm --filter @pace-radar/collector dev",
    "deploy:cf:api": "pnpm --filter @pace-radar/api run deploy",
    "deploy:cf:collector": "pnpm --filter @pace-radar/collector run deploy",
    "deploy:pages": "pnpm --filter @pace-radar/web run deploy",
    "version:patch": "pnpm version -r patch",
    "version:minor": "pnpm version -r minor",
    "version:major": "pnpm version -r major"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
